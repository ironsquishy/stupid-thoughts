import React from 'react';

const StupidPost = (_props) => {
    const { message, owner, responses, date, stpdHash } = _props;
    return (
        <React.Fragment>
            <div>Hello World</div>
        </React.Fragment>
    );
};

export default StupidPost;
